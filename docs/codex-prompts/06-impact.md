# Milestone 6 prompt — tests and impact

Add test relationships, secure source excerpts, and potential blast radius.

Detect direct JUnit calls and controlled naming conventions at different
confidence levels. Implement bounded reverse traversal to reachable callers,
endpoints, tests, and entities. Source reads must be limited to hash-matching
files in the active index.

Acceptance: fixture tests relate production code to tests; source traversal and
stale-content attacks are rejected; the UI labels reverse results as potential
impact rather than certainty.
