# Milestone 8 prompt — self-analysis release

Harden Code Atlas v0.1 for a clean public demo.

Seed a Compose-only self-analysis repository, grow Atlas Tasks into a compact
manually verifiable target, add Playwright and Compose smoke coverage, publish
architecture/confidence/pipeline/limitations documentation, add community and
security files, and record measured—not invented—performance results.

Acceptance: a clean `docker compose up --build` reaches healthy, automatically
indexes Code Atlas, exposes the indexing endpoint graph with source evidence,
passes Java/frontend/browser tests, and documents known limitations visibly.
