# Milestone 4 prompt — relationships

Implement project-local relationship analysis.

Resolve direct method calls, extends/implements, constructor injection, common
Spring Data methods, and entity reads/writes. Store confidence, resolution
method, source position, and evidence on every edge. Persist ambiguous,
unresolved, and external outcomes without inventing targets.

Acceptance: golden fixture relationships are deterministic; bounded endpoint
graph queries enforce depth/node/edge caps and report truncation.
