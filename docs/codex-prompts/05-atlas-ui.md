# Milestone 5 prompt — atlas UI

Build the endpoint atlas in React Flow with ELK layout.

Add endpoint browsing, typed nodes, confidence-aware edges, pan/zoom/selection,
depth and uncertainty controls, a source/evidence inspector, loading states, and
clear truncation warnings. Keep graph-heavy dependencies lazy-loaded.

Acceptance: selecting an endpoint renders a navigable bounded graph; selecting
a node or edge exposes identity, confidence, evidence, and exact source without
turning the UI into an editor.
